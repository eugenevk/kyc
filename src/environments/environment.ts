// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  GET_AUTH_TOKEN_PASSWORD: 'Orion123',
  GET_AUTH_TOKEN_USERNAME: 'mas.client',
  GRANT_TYPE: 'password',
  MAS_MATCHING_SERVICE: 'DataScreeningV3',
  MAS_MATCHING_STEP: 'execute_skel',
  OC_URL: 'api.opencorporates.com/v0.4',
  PASSWORD: 'Orion123',
  POSTGRES_USER: 'sas',
  POSTGRES_PASSWORD: 'Orion123',
  POSTGRES_DB: 'kyc',
  production: false,
  SERVER_NAME: 'rpcger01084.exnet.sas.com',
  USER_NAME: 'sas'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
